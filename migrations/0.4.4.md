# Migration 0.4.4

Applied by `job-tracker:health` when `config/.migrated-version` < `0.4.4`.

Each step is idempotent: check first, write only if the marker is absent.
Stop on first failure; do not update `config/.migrated-version` until all steps pass.

---

## Step 1 — Register source-registry and browser-patterns in the config zone

**Target file:** `config/paths.md`.

**Check:** this step is satisfied when `config/paths.md` contains the exact string `config/source-registry.md` in the `## Configuration Zones` section.
- If found -> skip.
- If not found -> apply.

**Apply:** in the `- **Config (\`config/\`):**` line under `## Configuration Zones`, append `, \`config/source-registry.md\`, \`config/browser-patterns.md\`` if either is missing from that line. Do not rewrite the rest of the file.

**Success condition:** `config/paths.md` lists both `config/source-registry.md` and `config/browser-patterns.md` in the `## Configuration Zones` line.

**Failure condition:** `config/paths.md` is missing, the `## Configuration Zones` line cannot be found, or either marker is absent after the write. Stop and report. Do not bump `.migrated-version`.

---

## Step 2 — Use slash-command form in next-actions examples

**Target file:** `config/next-actions.md`.

**Check:** this step is satisfied when `config/next-actions.md` contains the exact string `resume \`/job-tracker:run\` from the latest Session Report`.
- If found -> skip.
- If not found -> apply.

**Apply:**

1. Replace `resume \`job-tracker:run\` from the latest Session Report and execute the next internal step` with `resume \`/job-tracker:run\` from the latest Session Report and execute the next internal step`.
2. Replace `Next internal step: run \`job-tracker:company ExampleCo\`` with `Next internal step: \`/job-tracker:company ExampleCo\``.
3. Replace `- [r] Company Research (Recommended) — run \`job-tracker:company Acme\`` with `- [r] Company Research (Recommended) — \`/job-tracker:company Acme\``.
4. Replace `- [d] Draft Messages — prepare and save drafts with \`job-tracker:draft Acme\`` with `- [d] Draft Messages — prepare and save drafts with \`/job-tracker:draft Acme\``.

Only touch the example blocks; do not rewrite the rest of the file.

**Success condition:** `config/next-actions.md` contains `resume \`/job-tracker:run\` from the latest Session Report`.

**Failure condition:** `config/next-actions.md` is missing or the marker is absent after the write. Stop and report. Do not bump `.migrated-version`.

---

<!-- Add migration steps here -->
