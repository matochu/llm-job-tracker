# Migration 0.4.3

Applied by `job-tracker:health` when `config/.migrated-version` < `0.4.3`.

Each step is idempotent: check first, write only if the marker is absent.
Stop on first failure; do not update `config/.migrated-version` until all steps pass.

---

## Step 1 — Seed source registry

**Target file:** `config/source-registry.md`.

**Check:** this step is satisfied when `config/source-registry.md` exists.
- If found -> skip.
- If not found -> apply.

**Apply:**

1. In plugin mode, copy `$CLAUDE_PLUGIN_ROOT/config/source-registry.md` to `config/source-registry.md` if that source exists.
2. In workspace mode, copy the seeded workspace source `config/source-registry.md` if present after CLI update.
3. If no source is available, create the file with the following content:

```md
# Source Registry

This file owns source values, host patterns, and source-specific access policy. Skills should read this registry instead of hardcoding provider lists or source derivation tables.

## ATS Probe Providers

These providers are supported by `scripts/ats-probe.js` for discovery from public board/listing feeds. They are not liveness checks for a tracked direct job URL.

| Provider | Source value | Host patterns | Discovery feed | Liveness policy |
|---|---|---|---|---|
| `ashby` | `ashby` | `jobs.ashbyhq.com` | `https://api.ashbyhq.com/posting-api/job-board/[slug]?includeCompensation=true` | direct job URL via Browser MCP |
| `lever` | `lever` | `jobs.lever.co` | `https://api.lever.co/v0/postings/[slug]?mode=json` | direct job URL via Browser MCP |
| `greenhouse` | `greenhouse` | `*.greenhouse.io`, `job-boards*.greenhouse.io`, `boards.greenhouse.io` | `https://api.greenhouse.io/v1/boards/[slug]/jobs?content=true` | direct job URL via Browser MCP |
| `workable` | `workable` | `apply.workable.com` | `https://apply.workable.com/[slug]/jobs.md` | direct job URL via Browser MCP |
| `recruitee` | `recruitee` | `*.recruitee.com` | `https://[slug].recruitee.com/api/offers/` | direct job URL via Browser MCP |
| `smartrecruiters` | `smartrecruiters` | `*.smartrecruiters.com` | `https://api.smartrecruiters.com/v1/companies/[slug]/postings?limit=[limit]&offset=[offset]&status=PUBLIC` | direct job URL via Browser MCP |

## ATS Probe Search Defaults

`scripts/ats-probe.js` uses these defaults for discovery filtering. Profile hints can add more keywords, but final fit/reject decisions belong to skills and profile rules.

### Keywords

- frontend
- front-end
- front end
- product engineer
- fullstack
- full-stack
- full stack
- platform
- react
- typescript
- javascript
- design system

### Locations

- remote
- europe
- emea
- eu
- spain
- barcelona
- madrid
- portugal
- lisbon
- uk
- united kingdom
- ireland
- netherlands
- germany
- france

## Browser-Required Sources

Use Browser MCP for these sources. Do not replace them with plain web search when login, session state, filters, or JavaScript rendering are required.

| Source value | Host patterns / URLs | Why browser is required | Required access | Policy |
|---|---|---|---|---|
| `linkedin` | `*.linkedin.com` | login/session-dependent search, people, filters, Easy Apply | Playwright MCP with the user's logged-in account/session | never use API, WebFetch, curl, or web search as a substitute; open in Playwright and wait for user login when needed |
| `djinni` | `djinni.co` | login/session-dependent search, filters, private dashboard state | Playwright MCP with the user's logged-in account/session | never use API, WebFetch, curl, or web search as a substitute; open in Playwright and wait for user login when needed |
| `vc-board` | portfolio boards with JavaScript filters | dynamic filters and stale aggregate listings | Browser MCP when filters/rendering require it | use Browser MCP for discovery; verify final role at company/ATS source |
| `custom-board` | company-specific JavaScript boards | rendered content or custom redirects | Browser MCP when static fetch is incomplete | use Browser MCP when static fetch is incomplete |

## Source Derivation

`job-tracker:import` derives the tracker `Source` value from the canonical URL host using this table. If no pattern matches, use the bare root domain, for example `careers.acme.com` -> `acme`.

| Host pattern | Source value |
|---|---|
| `jobs.ashbyhq.com` | `ashby` |
| `jobs.lever.co` | `lever` |
| `*.greenhouse.io`, `job-boards*.greenhouse.io`, `boards.greenhouse.io` | `greenhouse` |
| `apply.workable.com` | `workable` |
| `*.recruitee.com` | `recruitee` |
| `*.smartrecruiters.com` | `smartrecruiters` |
| `*.teamtailor.com` | `teamtailor` |
| `jobs.wellfound.com`, `wellfound.com` | `wellfound` |
| `otta.com`, `app.otta.com` | `otta` |
| `*.linkedin.com` | `linkedin` |
| `djinni.co` | `djinni` |

## Tracker Source Values

Common tracker `Source` values:

- `linkedin`
- `djinni`
- `ashby`
- `lever`
- `greenhouse`
- `workable`
- `recruitee`
- `smartrecruiters`
- `teamtailor`
- `wellfound`
- `otta`
- `vc-board`
- `custom-board`
- `network`
- `hidden-market`
- `watchlist`

Add new reusable source values here before using them in tracker rows or skill docs.
```

**Success condition:** `config/source-registry.md` exists and contains `## ATS Probe Providers`, `## ATS Probe Search Defaults`, `## Browser-Required Sources`, `## Source Derivation`, `djinni.co`, and `Playwright MCP with the user's logged-in account/session`.

**Failure condition:** the file is still missing or does not contain the required markers after the write. Stop and report. Do not bump `.migrated-version`.

---

## Step 2 — Normalize Ashby discovery feed

**Target file:** `config/source-registry.md`.

**Check:** this step is satisfied when `config/source-registry.md` contains the exact string `https://api.ashbyhq.com/posting-api/job-board/[slug]?includeCompensation=true`.
- If found -> skip.
- If not found -> apply.

**Apply:** replace this exact older feed if present:

```md
https://api.ashbyhq.com/posting-api/job-board/[slug]
```

with:

```md
https://api.ashbyhq.com/posting-api/job-board/[slug]?includeCompensation=true
```

Only replace the Ashby feed in the `## ATS Probe Providers` table. Do not rewrite or reorder the rest of the file.

**Success condition:** `config/source-registry.md` contains `https://api.ashbyhq.com/posting-api/job-board/[slug]?includeCompensation=true`.

**Failure condition:** `config/source-registry.md` is missing, the older feed cannot be found, or the success marker is absent after the write. Stop and report. Do not bump `.migrated-version`.

---

## Step 3 — Add source registry references to source strategy

**Target file:** `strategy/sources.md`.

**Check:** this step is satisfied when `strategy/sources.md` contains the exact string `config/source-registry.md`.
- If found -> skip.
- If not found -> apply.

**Apply:**

1. Insert this paragraph immediately after the opening paragraph of the file:

```md
For source values, host patterns, ATS probe providers, and browser-required source policy, read `config/source-registry.md`. Follow each source's `Required access` policy; LinkedIn and Djinni require Playwright MCP with the user's logged-in account/session.
```

2. Under `## 2. ATS Direct Discovery`, after the paragraph that starts `Treat ATS APIs as the highest-signal discovery layer.`, insert:

```md
Provider IDs, host patterns, and source values are configured in `config/source-registry.md` under `## ATS Probe Providers`. Keep that registry aligned with `scripts/ats-probe.js`; do not duplicate provider ownership in skill files.
```

**Success condition:** `strategy/sources.md` contains `config/source-registry.md`.

**Failure condition:** `strategy/sources.md` is missing, the opening paragraph cannot be found, or the marker is absent after the write. Stop and report. Do not bump `.migrated-version`.

---

## Step 4 — Point tracker source values to source registry

**Target file:** `config/tracker-schema.md`.

**Check:** this step is satisfied when `config/tracker-schema.md` contains the exact string `config/source-registry.md`.
- If found -> skip.
- If not found -> apply.

**Apply:** under `## Table Requirements`, append:

```md
The `Source` column records where the lead came from. Common source values and URL host derivation rules live in `config/source-registry.md`. Use `network` when the lead was discovered via `job-tracker:find network`. Leave blank or omit only when the source is obvious from URL or ATS.
```

If an older paragraph in the same section starts with `The \`Source\` column records where the lead came from. Common values:`, replace that paragraph with the text above instead of appending.

**Success condition:** `config/tracker-schema.md` contains `config/source-registry.md`.

**Failure condition:** the file is missing or the marker is absent after the write. Stop and report. Do not bump `.migrated-version`.
